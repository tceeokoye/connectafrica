import { ReactNode } from 'react'

export const metadata = {
  title: 'Medical Container Campaign',
  description: 'Details about the Medical Container Campaign.'
}

export default function MedicalContainerLayout({
  children,
}: {
  children: ReactNode
}) {
  return children
}
