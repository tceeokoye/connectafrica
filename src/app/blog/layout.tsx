import { ReactNode } from 'react'

export const metadata = {
  title: 'Blog — Connect with Africa',
  description: 'Stories and impact updates from Connect with Africa.'
}

export default function BlogLayout({
  children,
}: {
  children: ReactNode
}) {
  return children
}
