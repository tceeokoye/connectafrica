import { ReactNode } from 'react'

export const metadata = {
  title: 'Blog — Connect Africa',
  description: 'Stories and impact updates from Connect Africa.'
}

export default function BlogLayout({
  children,
}: {
  children: ReactNode
}) {
  return children
}
