import { ReactNode } from 'react'

export const metadata = {
  title: 'Campaigns — Connect with Africa',
  description: 'Active and past campaigns.'
}

export default function CampaignsLayout({
  children,
}: {
  children: ReactNode
}) {
  return children
}
