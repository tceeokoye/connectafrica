import { ReactNode } from 'react'

export const metadata = {
  title: 'Campaigns — Connect Africa',
  description: 'Active and past campaigns.'
}

export default function CampaignsLayout({
  children,
}: {
  children: ReactNode
}) {
  return children
}
